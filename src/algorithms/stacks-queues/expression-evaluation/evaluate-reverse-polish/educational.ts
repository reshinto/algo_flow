import type { EducationalContent } from "@/types";

export const evaluateReversePolishEducational: EducationalContent = {
  overview:
    "**Evaluate Reverse Polish Notation** (RPN), also called postfix notation, is an expression format where operators follow their operands. For example, `2 1 + 3 *` means `(2 + 1) * 3 = 9`.\n\nA stack is the natural data structure for this: push every number, and when an operator is encountered, pop two operands, compute, and push the result back. No parentheses or precedence rules are needed — the notation encodes order of operations directly.",

  howItWorks:
    "The algorithm scans left to right through the token array:\n\n" +
    "1. **Number token** → parse and push onto the operand stack.\n" +
    "2. **Operator token** (`+`, `-`, `*`, `/`):\n" +
    "   - Pop the top two values: `operandB` (last pushed), then `operandA`.\n" +
    "   - Compute `operandA operator operandB`.\n" +
    "   - Push the result back onto the stack.\n" +
    "3. **End of tokens** → the single value remaining on the stack is the answer.\n\n" +
    '### Example trace on `["2", "1", "+", "3", "*"]`\n\n' +
    "```\n" +
    "token  action              stack\n" +
    "2      push                [2]\n" +
    "1      push                [2, 1]\n" +
    "+      pop 1, pop 2        []\n" +
    "       compute 2 + 1 = 3\n" +
    "       push 3              [3]\n" +
    "3      push                [3, 3]\n" +
    "*      pop 3, pop 3        []\n" +
    "       compute 3 * 3 = 9\n" +
    "       push 9              [9]\n" +
    "end    return stack[0] = 9\n" +
    "```\n\n" +
    "Division truncates toward zero (matching LeetCode 150's requirement), so `-7 / 2 = -3`, not `-4`.",

  timeAndSpaceComplexity:
    "**Time Complexity: `O(n)`**\n\n" +
    "Each token is visited exactly once. Each push and pop is `O(1)`, so total time is linear in the number of tokens.\n\n" +
    "**Space Complexity: `O(n)`**\n\n" +
    "In the worst case (all operands, no operators) the entire token list is pushed onto the stack, using `O(n)` extra space.",

  bestAndWorstCase:
    '**Best case** — a single-token expression like `["5"]`: one push and one return, `O(1)` work.\n\n' +
    '**Worst case** — all tokens are operands followed by operators (e.g. `["1", "2", "3", "+", "+"]`): every token is processed in sequence, giving `O(n)` time and up to `O(n/2)` stack depth.\n\n' +
    "The algorithm always completes in a single linear pass — there is no early termination path.",

  realWorldUses: [
    "**Stack-based virtual machines:** The Java Virtual Machine (JVM) and CPython interpreter use postfix-like bytecode internally to evaluate expressions.",
    "**Scientific calculators:** Classic HP RPN calculators use postfix notation, letting users avoid parentheses by entering operations in execution order.",
    "**Compiler back-ends:** After parsing an infix expression into an AST, many compilers emit postfix instructions for easier machine-code generation.",
    "**Spreadsheet formula engines:** Some engines convert user-entered infix formulas to RPN internally for efficient evaluation.",
  ],

  strengthsAndLimitations: {
    strengths: [
      "O(n) time — single pass with no backtracking or lookahead.",
      "No parentheses needed — precedence is implicit in token order.",
      "Simple, branch-minimal loop body with no recursion.",
    ],
    limitations: [
      "O(n) stack space in the worst case.",
      "Assumes well-formed input — invalid RPN (too few operands for an operator) is undefined behavior without extra validation.",
      "Less human-readable than standard infix notation; requires preprocessing to generate from user-entered expressions.",
    ],
  },

  whenToUseIt:
    "Use this pattern whenever you need to evaluate a postfix expression or process an operator stream that was generated by an infix-to-postfix conversion (shunting-yard algorithm). If you are building a full expression evaluator that accepts infix input, first convert to RPN, then apply this algorithm. For single-operator precedence levels (only + and -), a simpler left-to-right scan without a stack is sufficient.",
};
